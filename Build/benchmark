#!/usr/bin/env python3
"""Local performance capture and report comparison."""
from pathlib import Path
import sys

sys.path.insert(0, str(Path(__file__).resolve().parent / "Benchmarks"))
from benchmark import main

raise SystemExit(main())
