#if SWIFT_PACKAGE
import Foundation

/// Shared media and fixtures for examples and tests.
public enum TestResources {
    /// The bundle containing the shared media and fixtures.
    public static let bundle = Bundle.module
}
#endif
